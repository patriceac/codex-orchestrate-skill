interface:
  display_name: "Orchestrate"
  short_description: "Coordinate autonomous bounded parallel work"
  default_prompt: "Use $orchestrate to proceed autonomously within the authorized scope: delegate only independent work inside a bounded envelope, keep architecture, security, and integration on the main agent, and ask only when new authority or an enforced approval is genuinely required."

policy:
  allow_implicit_invocation: true
