interface:
  display_name: "Orchestrate"
  short_description: "Coordinate bounded parallel work"
  default_prompt: "Use $orchestrate with a bounded worker and review envelope: delegate only independent work, reuse and close lanes, keep architecture, security, and integration on the main agent, and stop when acceptance criteria pass."

policy:
  allow_implicit_invocation: true
