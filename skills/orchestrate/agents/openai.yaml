interface:
  display_name: "Orchestrate"
  short_description: "Delegate bounded work to parallel agents"
  default_prompt: "Use $orchestrate to split this substantial task into safe, independent workstreams and integrate the results."

policy:
  allow_implicit_invocation: true
